--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy>=2.0
numpy>=1.26
pandas>=2.2
yfinance>=0.2.40
websockets>=13.0
python-dotenv>=1.0
httpx>=0.27
pytest>=8.3
pytest-asyncio>=0.24

# Desktop build only (build_exe.bat / desktop_launcher.py): opens the
# dashboard in its own native window instead of a browser tab. Not used by
# the web deploy (requirements-web.txt).
pywebview>=5.0

# News sentiment (FinBERT, runs locally, no API key). Optional in spirit:
# if these fail to install or load, app/news/sentiment_analyzer.py degrades
# to SentimentLabel.UNAVAILABLE instead of crashing anything.
torch>=2.0
transformers>=4.40
